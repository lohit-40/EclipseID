
services:
  proof-server:
    image: midnightntwrk/proof-server:8.1.0
    container_name: midnight-proof-server
    ports:
      - "6300:6300"
    environment:
      - RUST_BACKTRACE=full
      - RUST_LOG=trace
    restart: unless-stopped
